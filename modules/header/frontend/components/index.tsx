import * as React from "react";
import * as ReactDOM from "react-dom";

import MainContents from "./main_contents";

const rootElem = document.getElementById('top-header');

ReactDOM.render(
  <MainContents />,
  rootElem
);
